---
name: weekly-matchup
description: >
  매주 일요일 테니스 동호회(귀뚜라미 클린 테니스 코트, K·L 2면, 8명, 3타임)
  대진표를 생성·확정·기록합니다. 사용자가 참석자 이름을 나열하거나
  "대진표 짜줘", "이번 주 참석자", "코트 운영" 등을 말하면 이 스킬을 사용하세요.
---

# 주간 대진표 생성

## 1. 참석자 확인

사용자가 준 이름을 `data/members.csv`와 대조합니다.

- 인원은 **항상 8명**입니다. 8명이 아니면 진행하지 말고, 빈 자리를 게스트로
  채울 것인지 물어보세요.
- **명단에 없는 이름**은 게스트입니다. **성별과 실력(레벨)을 먼저 물어보세요.**
  추정하지 마세요. 확인 후 `--guest "이름:남:B"` 로 넘깁니다.
- 게스트가 다음에도 나온다면 `data/members.csv`에 추가할지 물어보세요.

## 2. 생성

```bash
python3 scripts/matchup.py generate --date YYYY-MM-DD <이름 8개> [--guest "이름:성별:레벨"]
```

`--date`를 생략하면 히스토리에 없는 다음 일요일이 자동 선택됩니다.

## 3. 사용자에게 보여주기

**후보 1번의 대진표 본문**을 그대로 보여주고, 그 아래 균형 분석을 요약합니다.
후보 2·3은 "다른 안도 있습니다" 정도로만 언급하고, 요청 시 펼칩니다.

체크해서 같이 알려줄 것:
- 실력 분리 타임이 몇 타임인지
- 팀 점수 차가 2점 이상인 코트가 있으면 그 사실과 이유
- 파트너 중복 여부
- 게스트가 있으면 어떤 실력으로 계산했는지

## 4. 확정

사용자가 승인하면:

```bash
python3 scripts/matchup.py save --pick 1
```

그리고 `data/history.md`를 커밋·푸시합니다.
승인 전에는 절대 save 하지 마세요.

사용자가 직접 고친 대진표를 주면 **추측하지 말고 채점하세요.**

```bash
python3 scripts/matchup.py score <파일> <이름 8개> [--guest "이름:성별:레벨"]
```

후보와 벌점을 항목별로 비교해서 어디서 좋아지고 어디서 나빠졌는지 알려주고,
판단은 사용자에게 맡기세요.

수정 요청("A와 B는 붙여줘" 등)이 오면 `--seed` 값을 바꿔 다른 후보를 뽑거나,
후보 2·3을 제안하고, 그래도 안 맞으면 대진표를 직접 손보되
**표기 규칙(`K코트: a b vs c d`, 점수 높은 팀이 왼쪽)**을 지켜서 history.md에 기록하세요.

## 참고

- 규칙과 가중치: `docs/rules.md`
- 가중치 조정 요청이 오면 `scripts/matchup.py` 상단 `W_*` 상수를 수정합니다.
